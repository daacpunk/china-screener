# Web framework
fastapi>=0.115,<0.138
uvicorn[standard]>=0.30,<0.50
jinja2>=3.1,<4
python-multipart>=0.0.9

# Data / indicators
# pandas_ta 0.4.71b0 requires numpy>=2.2.6 which in turn pulls pandas 3.x.
# These three are mutually compatible and verified working together in CI/sandbox.
pandas>=2.2,<3.1
numpy>=2.2.6,<2.3
pandas_ta==0.4.71b0

# File parsing / docs
openpyxl>=3.1,<4
markdown>=3.6,<4

# Secrets
cryptography>=42,<50

# LLM / HTTP
httpx>=0.27,<0.29
anthropic>=0.40,<1

# Tests
pytest>=8,<10
