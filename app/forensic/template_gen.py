"""Forensic Excel template generation (Phase E, deferred): build a standardized
FactSet forensic line-item workbook (openpyxl + real dictionary codes + optional
VBA helper) for the user to populate and re-upload, mirroring weekly template_gen."""
